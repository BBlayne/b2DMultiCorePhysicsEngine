#pragma once
#include <vector>
#include "bBody.h"
#include "bManifold.h"

class Broadphase {
private:

public:
	//static void GeneratePairs(std::vector<bManifold> &contacts, std::vector<bBody> bodies /* threadID */);
};